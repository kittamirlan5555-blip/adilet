# maps/ — карты и конфигурация линковки

- `codes.json` — реестр документов корпуса: слаг → doc_id (НГР), title,
  опц. `source` (имя файла выгрузки). РЕДАКТИРУЕТСЯ РУКАМИ при добавлении.
- `npa_mapping.json` — фраза-ключ (точный падеж) → НГР внешнего акта.
  Новый ключ вносится ТОЛЬКО после сверки НГР на adilet (§5 CLAUDE.md).
- `manual_overrides.json` — точечные ручные оверрайды (скрипт 16).
- `article_map_{слаг}.json` / `subpoint_map_{слаг}.json` — статья/пункт →
  якорь; ГЕНЕРИРУЮТСЯ пайплайном (шаги 01/07), руками не править.

Ежегодно в декабре: обновить НГР закона «о республиканском бюджете»
(сейчас Z2500000239) — §2.1 CLAUDE.md.
