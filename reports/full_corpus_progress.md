# Прогон полного корпуса РК — прогресс по батчам

Ветка `full-corpus`. Цель — субстантивные действующие акты (Закон/Кодекс/Конституц.закон
+ 4 указа-закона). Реестр: `maps/corpus_registry.json` (TODO=179). Правила остановки:
`merges>0` ИЛИ `prefix-дефект>0` ИЛИ `text-invariance FAIL` ИЛИ `GREEN<90%` → стоп+разбор.

Аудит независимый (`pilot/audit_batch.py`, из сырого HTML). Сэмплы —
`reports/samples/batch_NNN/` (5 случайных `_ready.html`, канал внешней проверки).

| батч | ингест OK | REPEALED | quarantine/empty | driver DONE | GREEN | merges | prefix | cov | вердикт |
|---|--:|--:|--:|--:|--:|--:|--:|--:|:--:|
| 001 | 95 (+3 fallback) | 2 | 3+1 | 89 | 92/95 | 0 | 0 | 91% | ✅ |
| 002 | 75 (72+3 fallback) | 0 | 5+2 | 67 | 70/75 (93%) | 0 | 0 | 89% | ✅ |
| 003 (дожим) | 2 | 0 | 0 | 2 | 2/2 | 0 | 0 | — | ✅ |

**Стоп-правила по обоим батчам: НЕ сработали** (merges=0, prefix=0, text-inv OK, GREEN≥90%).
Прогон завершён — весь TODO покрыт (batch_001=100 + batch_002=82 = 182 попыток на 179 TODO
+ 3 повторные no-docid).

> **UPD дожим D1:** диагноз «клиентский рендер» ОПРОВЕРГНУТ headless-проверкой
> (playwright, `pilot/03_headless_fetch.py`): рендер даёт тот же ~4-5К текст. Эти акты
> реально ТОНКИЕ/нестандартные (пункты вместо статей, каталоги) — пофайловые обоснования
> в `reports/full_corpus_summary.md` §4. Раздел ниже — историческая запись.

### FETCH_THIN (клиентский рендер adilet — раздельно, deferred) [УСТАРЕЛО, см. UPD]
Батч-002 ингест: **7 FETCH_THIN** — raw-GET возвращает страницу БЕЗ тела статей (0 «Статья»,
0 якорей, ~4 КБ текста): старые акты 1991-96, adilet рендерит их клиентом (JS). Среди них —
**все 4 указа-закона** (`U950002147_`, `U950002367_`, `U950002457_` «О столице РК», `U960002788_`),
которые владелец просил включить, + 3 старых Z (`Z910003900_`, `Z930002500_`, `Z940000119_`).
**Не обработаны** (нужен playwright/headless — отдельная инфра, не в этом прогоне). Итого по
корпусу 7 таких. `U950002367_` — вдобавок homoglyph-repealer («пpизнании» латинская p).
