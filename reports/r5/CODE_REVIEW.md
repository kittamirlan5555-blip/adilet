# R5 — код-ревью живых скриптов (pipeline/ + audit/ + verify/structurize)

Объём: 62 живых скрипта (24 pipeline, 33 audit, verify.py, paths.py,
audit_links_coverage.py, structurize.py). Метод: автоскан анти-классов
(tmp-скрипт, в репо не входит) + построчное чтение зон риска. После правок:
34/34 юнит-теста OK, 69 raw-nested по корпусу = 0, verify.py --all PASS
(см. финальный коммит). Поведение канона не менялось — изменения только
в обработке ОШИБОЧНЫХ путей (раньше молча, теперь WARN/abort) и в путях
отчётов (дерево v2).

## Исправлено (по анти-классам)

| скрипт | находка | исправление |
|---|---|---|
| **audit_links_coverage.py** | КРИТИЧНО: остался на data/+config/ — сломан с Фазы A; его импортируют канонические 28/29/30/43 (их запуск падал бы) | переведён на paths.py; src_path читает поле source из codes.json |
| auditlib.py | дубль-словарь SOURCE_NAME (класс рассинхрона с codes.json) | строится из поля source в codes.json (единый источник) |
| maps/codes.json | имена выгрузок grazhdanskiy_osob/informatizacii жили только в коде | добавлены поля source |
| 02_fix_internal_links.py | отказ замены инвариант-гейтом фиксировался только в CSV (SKIPPED_INVARIANT) — в консоли молча | + WARN в stderr; докстринг дополнен |
| 08_fix_selfref_subpoints.py | ТИХИЙ FALLBACK: нераспознанный разделитель перечня молча подменялся «, » (класс порчи диапазонов); записи без файлового гейта | sep не распознан -> перечень пропущен + WARN; + файловый text-invariance-гейт §6.1 перед записью |
| 17_normalize_link_targets.py | запись в final/ без §6.1-гейта | + гейт (FAIL -> запись отменена, exit 1) |
| 18_strip_links_in_notes.py | то же | + гейт |
| 28_fix_link_correctness.py | то же (оба файла форм) | + гейт на обе записи |
| 31_add_lost_links_structured.py | то же | + гейт |
| chunk_npa.py | except Exception: pass вокруг парса номера статьи КоАП — нераспознанный номер молча давал unit_type=«пункт» | явная проверка + WARN в stderr |
| pipeline.py, 43_dobivka | широкие except Exception на platform-гардах | сужены до (AttributeError, ValueError, OSError) с комментарием |
| 69_sixcheck_laws.py | nested-проверка через find_parent("a") СЛЕПА: html.parser автозакрывает вложенные <a> при парсинге | продублирована сырым re-сканом глубины (как в 71_gates); прогон по корпусу: nested=0 |
| paths.py | кириллица в print ломалась на cp866-консоли без PYTHONIOENCODING (платформенная ловушка каждого запуска) | мягкий reconfigure stdout/stderr в utf-8 при импорте (задокументированный side-effect) |
| 64/67/69/74/75/71_gates/76/68/71_fullspan/72/73/70/pipeline/verify/structurize | пути отчётов | -> reports/gates/ через paths.GATES (дерево v2) |

## Проверено — чисто (без правок)

- Тихие fallback'и в 02 (8 мест `sep_m ... else`): фактически НЕЙТРАЛИЗОВАНЫ
  центральным _vis-гейтом (подмена отвергается, фиксируется SKIPPED_INVARIANT)
  + файловым гейтом перед записью; теперь ещё и WARN. Не переписывались —
  боевой 80КБ скрипт, поведение запинено tests/test_02_heal.
- Голые `except:` — НЕ найдены; широких `except Exception` было 3 (см. выше).
- Запись в final/ с гейтом уже была: 16, 29, 30, 43, 68, 71, 72, 73, r2_03,
  r3_02/04/05/06/07, r4_00/01, f01–f04 (f-серия — «инвариант»-гейты).
- Хардкоды data/+config/ вне attic — после фикса audit_links_coverage: 0
  (фолбэк data/ в 64 — намеренный, для легаси-бэкапа, с комментарием).
- Докстринги-шапки: есть у всех живых скриптов (вход/выход/гейты);
  усилены у 02 и 08.
- BS-vs-raw вложенность: 71_gates всегда был raw; 69 продублирован;
  r3_02/r3_04/r4_* — raw-сканы глубины внутри гейтов.

## Не трогалось намеренно

- scripts/attic/ — заморожен (исторические, пути не чинятся).
- 13_cleanup_html (флэттенер вложенных <a> — трансформер, его выход держат
  raw-гейты 71_gates/69 ниже по конвейеру).
- deliverables/law_kit — поставляемый снапшот (обновляется при передаче).
