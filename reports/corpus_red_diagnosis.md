# Диагностика корпусных RED (71_gates G5 + 75_crosscode) — READ-ONLY

_Ветка `full-corpus`, дожим D5. НИЧЕГО не чинилось — только разбор. Решение за владельцем._

## Симптом

Pre-push verify по всем 43 корпусным слагам (впервые сработал на миграции данных):
- `71_gates --strict`: **RED у 7 доков** (grazhdanskiy, grazhdanskiy_osob, koap,
  obrazovanie, predprinimatel, zemelnyy, zhilishniy) — все по **G5 «cross-code якоря»**;
  G1-G4, G6 — GREEN у всех.
- `75_crosscode`: MISMATCH=3, UNRESOLVED=53.

## Расследование (пруфы)

**11 битых пар** (док → цель#якорь): grazhdanskiy→`Z1400000176#z446`,
grazhdanskiy_osob→`Z2200000178#z6`,`Z2300000021#z0`, koap→`Z000000085_#z17-1`,
`Z1400000188#z1436`, obrazovanie→`Z1500000314#z322`, predprinimatel→`Z1400000202#z248`,
`Z2400000103#z0`, zemelnyy→`Z1700000047#z44`,`Z2500000165#z0`, zhilishniy→`Z2300000014#z0`.

1. **Все 11 целей — законы, обработанные нами в пилоте/батчах** (проверено по манифестам).
   До пилота их файлов в `final/` НЕ БЫЛО → G5 физически не мог проверить эти ссылки
   (нечего сравнивать) → корпус проходил гейты зелёным.
2. **Якорей нет НИ в старой сборке (снимок v1), НИ в новой** (проверены Z1400000188#z1436,
   Z000000085_#z17-1, Z2300000021#z0, Z1400000176#z446, Z1700000047#z44 — все False в обеих).
   Т.е. ссылки НЕ «сломались от пересборки» — они изначально строились НЕ под наши сборки.

## Вердикт

**Гейты НЕ протухли — G5/75 работают верно.** И это НЕ дефект, внесённый в принятый корпус
сейчас: это **открытый архитектурный вопрос §9 CLAUDE.md**, ставший видимым. Корпусные
cross-code ссылки на статьи целевых законов строились под **сетку живого adilet (модель B)**
в момент, когда локальных сборок целей не существовало. Наши сборки (модель A) имеют ДРУГУЮ
якорную сетку. Как только пилот/батчи создали локальные файлы целей, G5 впервые сравнил —
и честно показал несовпадение моделей.

Отдельно: 4 ссылки на `#z0` — подозрительны даже для живого adilet (нулевой якорь);
вероятно, дефект построения ещё старого раунда. Мал (4 шт.), входит в тот же разбор.

## Варианты решения (владельцу; НЕ делал)

| модель деплоя (§9) | что делать с 11+53 ссылками |
|---|---|
| **A: деплоятся НАШИ файлы** | пере-резолв якорей по НАШИМ картам целей (75 re-resolve; данные для этого теперь есть — цели собраны) |
| **B: ссылки на живой adilet** | оставить как есть; G5 научить различать «цель = живой adilet» (exempt-список или сверка с live-снимком); `#z0` всё равно править |
| до решения | G5-RED по этим 7 докам считать «известным §9-классом» (не блокер сдачи батчей: они `final/` не трогают) |
