# Ночная сессия — заметки (ветка pilot-100b)

Автономный прогон по списку из 4 пунктов. Корпус (final/ 38, codes.json 43) не трогаю;
scripts/vector/ не запускаю. Каждый фикс — с пруфом из файлов.

## Сделано / отложено / найдено

### 1a. gate_pass — GATE_RED теперь достижим ✅
Причина no-op: `run()` отдаёт ПОСЛЕДНЮЮ строку 69 = `written: … clean=True|False`,
а старый `gate_pass` искал в ней слова-провалы (их там нет) → всегда GREEN.
Фикс: парсим `clean=false`→RED, `clean=true`→GREEN, иначе ERR (не ложный GREEN).
**Пруф:** чистый док Z980000258_ → GREEN; тот же док с вставленной висячей `#z9999999`
внутри → **RED**; файл восстановлен (0 маркеров осталось).

### 1d. Гард корпуса в 02_batch_run ✅
`corpus_keys()` = ключи `git show HEAD:maps/codes.json` (43); при старте отказ, если
в списке слагов есть любой из них (перезапись их final/ = порча корпуса).
**Пруф:** `02_batch_run.py nalog` → ОТКАЗ (1 слаг); `--all` → ОТКАЗ (38 корпусных с
source на диске); пилотный `Z980000258_ --steps pre` → идёт (не отказ). --all-футган закрыт.

### 1b. Repeal — убрана ловушка причастия ✅
`утрати\w*\s+силу` → `[Уу]тратил[аио]?\s+силу` (в 01_ingest и collect_new). Финитный
глагол = статус ЭТОГО акта; причастие «утративш…» (про другие акты, «Признать
утратившими силу…») больше не флагает.
**Пруф (дифф OLD vs NEW по 260 source):** `reports/pilot/repeal_diff.md` — **0
изменившихся вердиктов**. Т.е. на реальных данных NEW==OLD (все 22 отменённых имеют
финитную форму), фикс убирает только ЛАТЕНТНОЕ ложное срабатывание. NEW ловит 18/18
манифест-REPEALED, включая Z020000302_/Z040000528_; действующие (Z980000258_,
Z1600000486) не флагаются.

### 1c. Repeal — сноска НИЖЕ первой «Статьи» — ОТЛОЖЕНО (не чинил) ⏸
Надёжного признака нет. Полнотекстовый поиск финитного «утратил силу» добавляет 7 доков,
но 4 из них — ДЕЙСТВУЮЩИЕ корпусные кодексы (appk/arbitrazh/konstsud/lesnoy: упоминают
«утратил силу» в теле про отменённые нормы) → ложные срабатывания. Оставил детект по
шапке; в коде 01_ingest — комментарий с обоснованием. Детали — в repeal_diff.md.

### 2. Происхождение склеек «цифра+статьи» (НЕ чинил) — диагноз
Вопрос: «пункта 3статьи 6» — уже в сыром source или появляется на шаге пайплайна?
**Ответ: появляется на шаге `02_fix_internal_links.py`.** Трассировка pipeline.py
--keep по Z000000126_ (счётчик `\d+стать` в get_text без разделителя):
| стадия | склеек |
|---|--:|
| source (сырьё adilet) | 0 |
| 07 add_subpoint_anchors (anchored) | 0 |
| 10 cross_code_refs (xref) | 0 |
| **02 fix_internal_links (fixed)** | **5** ← первое появление |
| 03 external / 06 finalize / 13 cleanup / canon | 5 (переносится дальше) |
Механизм: 02 оборачивает внутреннюю ссылку «пункт/подпункт N статьи M» в `<a>` и
теряет пробел между числом-ссылкой и «статьи» → `2</a><a href=…#z338>статьи` (get_text
без разд → «2статьи»). В корпусе тот же класс: koap `_structured` — «частью 1-2статьи
441» внутри одного `<a href="#z4101">` (пробел проглочен при оборачивании спана).
В сыром source и после 11_structure склеек НЕТ (0). Правку не делал (по заданию —
только диагноз). Кандидат на фикс: вставлять пробел на границе спанов в 02 (или в
71 fullspan). Видимый дефект в браузере: «3статьи» рядом.

### 3. Косметика индекса ✅
`build_index.clean_title()` срезает хвост «- ИПС "Әділет"». Перегенерил
`reports/pilot/pilot_index.md` (0 вхождений «ИПС», #z total 3447 не изменился).
Пакет `deliverables/` НЕ трогал (сданный снимок).

---

## ИТОГ ночной сессии

**Сделано (4 коммита):**
- `cd1ed47` — gate_pass читает `clean=` (GATE_RED достижим, пруф RED на висячей #z) +
  гард корпуса (`--all`/`nalog` → ОТКАЗ).
- `6910655` — repeal-детект: финитный глагол вместо `утрати\w*` (убрана ловушка
  причастия), дифф OLD-vs-NEW по 260 source = 0 изменений (`repeal_diff.md`).
- `0b12ec1` — index: срез «- ИПС», перегенерация.
- (+ этот коммит: night_notes).

**Отложено (нет надёжного признака):**
- repeal (c) — сноска отмены НИЖЕ первой «Статьи». Полнотекст ложно флагает
  действующие кодексы (appk/arbitrazh/konstsud/lesnoy). Оставлено по шапке + комментарий.

**Найдено (диагноз, не чинил):**
- Склейки «цифра+статьи» рождаются в **`02_fix_internal_links.py`** (0 в source/11,
  5 после шага 02) — теряется пробел между числом-ссылкой и «статьи». Кандидат фикса —
  вставлять пробел на границе спанов. См. раздел 2.

**Замечено (не моё, не трогал):**
- `deliverables/anara_pilot100.rar` (3 МБ, mtime 07:32) — появился вне сессии (я .rar
  не делал; build_package пишет только папку). Не коммичу, не в .gitignore — оставил как есть.

**Корпус:** `final/` (38) и HEAD `codes.json` (43) не тронуты; `deliverables/anara_pilot100/`
(снимок) не тронут; `maps/codes.json` в рабочем дереве изменён (пилотные НГР runtime) — в
коммиты не идёт. scripts/vector/ не запускал.


---
## Сессия закрытия замечаний Анары (ветка anara-fixes)

### A2. Фикс склеек «пункт N статьи M» ✅ (коммит 1918f16)
Диагноз: шаг 02 при обёртке оставляет два соседних спана «N</a><a>статьи» без
пробела; гейт 02 whitespace-нечувствителен → пропускал. Источник (проверено): пробел
ЕСТЬ в исходнике adilet. Минимальный точечный фикс в 02 при сериализации:
`re.sub(r'(\d)(</a>)(<a…>стать[яиею])','\1\2 \3')`. Регресс 38 корпус+82 пилот:
23 изменившихся места в 12 файлах, ВСЕ класса «вернулся пробел», len(new)-len(old)==
число совпадений (больше ничего). End-to-end через canon: Z000000126_ склеек 5→0.

### A3. Автоген npa_mapping (главный рычаг) ✅
- Реестр название→НГР: codes.json + pool_new_meta (334) + adilet-ссылки из source
  (орфан «<a docs/НГР>Законом</a> … «Имя»», 325 имён).
- Извлёк из 82 _ready: 121 уникальное цитируемое закон-имя (306 вхождений, вне <a>).
- Закрыто из реестра: 57. Разрешено через adilet-ссылки: 50 → **fetch+<title>-сверка (§5)**:
  26 verified, 24 rejected (**18 отменённых** редакций — старый Налоговый K1700000120,
  О приватизации, О банкротстве и т.п. — НЕ линкуем на мёртвое; 6 title≠имя, напр.
  «О статусе»→«О статусе "Назарбаев Университет"»). Ещё 3 отброшены repeal-check по кэшу.
- **Влито 80 ключей (точные «…» подстроки) → npa_mapping 334→414, 0 конфликтов**,
  покрывают 251 вхождение. Все 10 Анариных актов закрыты (О языках Z970000151_,
  О гражданской защите Z1400000188, ОРД Z940004000_, страховой Z000000126_, …).
- Остаток unresolved (в отчёт): ~38 имён (24 rejected + 14 без НГР) — часть это
  отменённые редакции без найденной актуальной; правило «лучше недолинковать».
  Артефакты: reports/pilot/npa_{candidates,unresolved,resolved_candidates,verified,rejected}.json.

### БЛОКИРОВАНО: docx замечаний
Файл `Замечания_по_рандомным_ЗРК.docx` в репо НЕТ (положишь/скинешь). Блоки 1 (парсинг
жёлтых флагов), 5 (верификатор флагов), 6 (пакет v2 + anara_response) ждут docx.
Технич. ядро (A2 склейки + A3 маппинг) сделано без него.

### A4. Ре-прогон 82 (merge-fix + новый маппинг) ✅
Драйвер `02_batch_run.py` по 82 пилотным слагам (полный pipeline+canon+gate+chunk).
Итог: **80 DONE, 81/82 гейт GREEN**, 1 GATE_RED, 1 UNDER_CHUNK.
Независимый аудит `pilot/audit_rerun.py` (NEW final/ vs OLD-снимок пакета, §6):
- **text-invariance (ws-stripped): 0/82 нарушено** — ни merge-фикс, ни маппинг не тронули видимый текст.
- **merges `\d+стать`: 39 → 0** (фикс сработал end-to-end на всех 82).
- **внешние root-ссылки: 3396 → 3633 (+237)**; #z внутр 3447 (без изменений).
- **битых якорей #z: 0** (34 в первом прогоне — ложняк моего regex, обрезал под-пункт `#z80-1`→`z80`; чиню на полный фрагмент).
- вложенных `<a>` 0, двойных href 0.
Спот-чек 5 внешних (все с совпадением <title> §5): О языках→Z970000151_ root✓, О гражданской
защите→Z1400000188 root✓, ОРД→Z940004000_ root✓, О цифровых активах→Z2300000193 root✓,
О страховой деятельности→Z000000126_**#z91** (root=нет).
**2 пред-существующих квирка (OLD==NEW, НЕ регресс, в отчёт ревьюеру):**
- `Z1200000561` GATE_RED — эвристика 69 «B-list» на клаузе «статьи 44 и статьи 45, которые
  вводятся в действие»; обе статьи РЕАЛЬНО залинкованы (#z605,#z639 резолвятся). Ложный флаг.
- `«О страховой деятельности»→#z91` (2 линка: Z030000415_, Z030000474_) — кросс-код (шаг 10)
  увёл имя-целиком в списке актов на статью 91 вместо КОРНЯ (§4). Внутри ВЕРНОГО акта, не
  на чужой. Пред-существует в сданном пакете. Кандидат на правку 10/72 (риск для корпуса → отложено).
- `Z030000461_` UNDER_CHUNK 164 vs 162 — артефакт счёта: 2 div.article без data-num; РЕАЛЬНО
  чанки покрыли 162/162 разных статей. Не потеря.

### A1+A5. Флаги Анары из docx — распарсено и сверено ✅
`Замечания по рандомным ЗРК.docx` получен. Парсер жёлтых выделений
`pilot/verify_anara_flags.py` (zip→document.xml, runs с `w:highlight=yellow`) →
**19 флагов по 5 законам** → `maps/anara_pilot_flags.json`. Слаги: Об обеспечении
единства измерений `Z000000053_`, О биологической безопасности `Z2200000122`, О Службе
госохраны `Z950002483_`, Об автомобильном транспорте `Z030000476_`, О рынке ценных
бумаг `Z030000461_`.
- **14 внешних имён-актов** → CLOSED маппингом из A3 (полный спан → корень).
- **7 флагов «стать* 9-4»** (Z030000461_, ст.45/51/54×4/79): в полном тексте это
  «стать* 9-4 **Закона РК "О государственном регулировании … финансового рынка"**»
  (внешний `Z030000474_`), а НЕ внутренняя. **Статьи 9-4 в нашей выгрузке цели НЕТ**
  (класс «ст.380-3 УК») → по §4 + «minimum root link»: сдвинул левую границу уже
  существующего спана имени акта, поглотив «стать* 9-4» → полный спан на **корень
  Z030000474_** (`pilot/fix_flags_art94.py`, идемпотентно, 7 границ, text-invariance OK).
- **ИТОГ: 19/19 CLOSED** (посимвольная сверка in_a). Независимый аудит §6 после ручной
  правки Z030000461_ — ЧИСТО (nested 0, dbl-href 0, text-invariance 0/82).
- Пакет v2 пересобран (+`anara_pilot_flags.json`, обновлён `anara_response.md` §6).
  Сырой docx НЕ коммичу (§11: бинарь/метаданные) — лежит локально, в .gitignore.

---
## Ветка full-corpus (весь корпус РК, ~450-500 актов)

### B1. Спан-префикс «Законом РК» — влит в пайплайн ✅
Находка внешнего аудита: `03_find_external_npa` линкует только «"Название"», префикс
«Закон*/Кодекс* [Республики Казахстан|РК]» остаётся плейн-текстом СНАРУЖИ спана
(«Законом РК <a>"X"</a>» вместо корп. стандарта «<a>Законом РК "X"</a>»,
final/upk_ready.html). По пилоту: **211 таких дефектов** (406 уже правильных).
Фикс — `absorb_prefix()` в `72_external_root_link.py` (apply-фаза, до гейтов): по DOM
двигает левую границу внешнего КОРНЕВОГО `<a>` (спан начинается с кавычки-имени) влево,
поглощая префикс из соседнего текст-узла. Явные склонения «закон/кодекс», НЕ «\w*» —
иначе поглотило бы «Законодательством…» (описательный класс §4). 72 уже в canon-цепочке
драйвера и §7 → 400 доков не воспроизведут дефект.
**Регресс** (`pilot/regress_prefix_absorb.py`, копии в памяти, final/ не тронут):
пилот 82 — дефектов 211→0, поглощено 211, get_text sha инвариантен, nested=0; курируемый
корпус (upk/koap/grazhdanskiy/nalog/trudovoy/ugolovniy) — поглощено 0 (не переобёртываем
корректное). Идемпотентно (после — `<a>` начинается не с кавычки).
Принятый пилот НЕ перезаписывал (§ «не перезаписывать»); на любой будущей пересборке 72
починит те 211 автоматически.

### B2. Полный реестр корпуса РК ✅
adilet отдаёт listing по типам: `/rus/search/docs/pagesize=100&page=N&va=<ТИП>`
(ЗАК/КОД/КЗАК/УЗАК/УКОН). Каждая строка `div.post_holder`: НГР+title, `span.status`
(`status_yts`=Утративший силу / upd|new|err=действует), `<p>` дескриптор. Классификация
ИЗ LISTING — без покачки доков (`pilot/collect_registry.py`, 38 запросов, страницы
кэшированы `source/_registry_cache/`).
- **3748 уникальных актов**. Основной scope (ЗАК+КОД+КЗАК): repealed 362, поправки 1615,
  нерегуляторные 1392, done 129, **TODO субстантивных 179**.
- **Сверка с шефом 450-500:** сырых «действующих законов» ~1700, НО из них **1291
  ратификаций/договоров** (1-2 статьи, не линкуются) + 34 бюджета + 52 repealer
  («О признании утратившими силу») + 15 амнистий. **Субстантивных регуляторных = 308**
  (129 done + 179 TODO). Это НИЖЕ 450-500: оценка шефа, вероятно, широкая (с ратификациями)
  или округление; вдобавок «38 кодексов» завышено — на adilet всего 26 действующих кодексов.
- Пилот-82: 0 утечки в TODO, 0 не-в-реестре (done-матчинг корректен).
- **Остаток реальной работы ≈ 179 актов (~2 батча), не 400.** Вопрос владельцу: ратификации/
  бюджеты/edge-указы (4 УЗАК/УКОН, напр. «О столице РК») линкуем? По умолчанию — нет.
Артефакты: `maps/corpus_registry.json` (3748, поле bucket), `reports/corpus_registry.md`.

### B3. Раскладка данных на масштабе ✅ (миграция — по слову владельца)
Проблема: git-дерево ~477 МБ (`.git` ~316 МБ), 98 % — тяжёлое (deliverables 211,
derived 120, final 91, source 48). На 400+ актов — гигабайты, push не потянет.
Решение: код+карты+отчёты в git; source/final/derived/deliverables — на диске, а в git
манифест целостности `manifests/data_manifest.json` (path+size+sha256+mtime,
`scripts/data_manifest.py`, режим `--verify`). Собрано **1449 файлов / 967 МБ**,
манифест 284 КБ, verify сразу после генерации = ЦЕЛОСТНО.
`.gitignore` расширен на 4 каталога (для БУДУЩИХ файлов — прогон 400 не забьёт git).
`docs/DATA_LAYOUT.md` — схема, проверка, регенерация, миграция.
**НЕ выполнено (ждёт слова):** `git rm -r --cached source final derived deliverables`
(диск не трогает) и чистка истории `.git`. Сначала план+манифест — по правилу
«не удаляй без слова».

### B4. Первый батч-100 — прогон + аудит ✅
Реестр TODO нарезан (`slice_batches.py`) на 2 батча (100+75). Батч-001 (98 ЗАК + ГПК
+ 1 КЗАК, контаминанты уже отфильтрованы).
- **Ингест** (TLS-бандл, `--manifest`): 100/100, **95 OK**, 2 REPEALED (Z010000210_/
  Z070000300_ — детектор шапки поймал, хотя listing adilet врал «действует»),
  3 NO_DOCID (U-указ-законы 1995: страница без панельных self-ссылок — отложено).
- **Драйвер** (`--tag batch_001`, полный конвейер+канон): **89 DONE, гейт GREEN 92/95**;
  1 EMPTY (Z010000156_ — тонкий закон «о введении в действие», 0 статей), 3 QUARANTINE
  (0 статей/битая структура), 2 UNDER_CHUNK (off-by-1 счёт чанков).
- **Независимый аудит** (`audit_batch.py`, из сырого HTML): **merges 0 (A2), prefix-дефект
  0 (B1 держится на свежем выходе!), nested 0, битых #z 0, dbl-href 0.** Внешних root
  3240, #z внутр 1417, cov медиана 91%, gaps ~5.5/док (§4 hard-tail).
Пруф, что B1/A2 работают на масштабе: 211 prefix-дефектов пилота НЕ воспроизвелись.
**Остановлен на батче-1 — темп решает владелец (батч-002 = 75 доков, не запускал).**

### C1. Мелкие фиксы перед масштабом ✅
(а) `72.PREFIX_END`: добавлены склонения «ам/ах/ы» («законам/законах/законы»; «ами» ДО
«ам» в альтернации — «законами» не режется). Guard `NASTOYASH`: «настоящего/настоящим
Закон*» = само-ссылка (внутренняя), НЕ внешний «Законом РК "X"» — НЕ поглощаем (латентная
ловушка внешнего аудита). Регресс: пилот 212→0, корпус 0, text-invariant, nested=0;
юнит: «законам…"О банках"» поглощён, «настоящего Закона "X"» — нет. Детекторы
(audit_batch/regress) синхронизированы.
(б) `01_ingest` NO_DOCID→**OK_FALLBACK**: нет панельных self-ссылок, но sig>0 (структура
валидна) → doc_id=САМ НГР (adilet/docs/{НГР} валиден). Пруф на 3 U-указах-законах 1995
(О хоз.товариществах/О правовом положении иностранцев/Об ипотеке): sig 85/69/110 →
OK_FALLBACK, зарегистрированы. Идут в очередь корпуса.

### C3. Хвосты батча-1 (характеризация, не чинил) ⏸
- **Z010000156_** EMPTY — «О введении в действие Кодекса… об административных» — тонкий
  закон-ввод-в-действие (только «Статья 1. Признать утратившими силу…»). Корректно EMPTY,
  не цель (класс «о введении в действие», §5).
- **Z010000222_** QUARANTINE — «О Государственной премии мира и прогресса Первого Президента»
  — статут награды, нестандартная нумерация «Статья 1 1.» → 0 распознанных статей. Тонкий.
- **Z070000234_** QUARANTINE — «О бухгалтерском учёте и финансовой отчётности» — РЕАЛЬНЫЙ
  закон (28 <b>Статья), но заголовки вида «Статья N **.**» (ПРОБЕЛ перед точкой) → article_map
  ловит 3 из 28. **ФИКСИМЫЙ КЛАСС** (регэксп 11_structure/01_build_article_map: допустить
  `\s*\.`). НЕ чинил (риск для всего конвейера/корпуса) — кандидат на отдельный фикс с регрессом.
- **Z1600000024** QUARANTINE — «Об объёмах трансфертов общего характера…» — бюджетно-
  распределительный закон (4 короткие статьи), тонкий/бюджет-класс. Не цель линковки.
Итог: 3 из 4 — объективно тонкие/не-цель; 1 (Z070000234_) — ложный карантин из-за формата
«Статья N .», фикс отложен как отдельный класс.

### C4+C5. Прогон до конца + сводка ✅
Батчи по ~90: batch_001 (100) + batch_002 (82) покрыли весь TODO (179 + 3 повторных).
Оба ЧИСТО (стоп-правила не сработали): merges 0, prefix 0, text-inv OK, GREEN 97%/93%,
cov 91%/89%. Сэмпл-паки `reports/samples/batch_00N/`. Реестр done-статусы обновлены
(done_set читает batch_*.csv DONE/UNDER_CHUNK): **обработано 288/308 субстантивных (~95%)**.
Остаток 20 — все edge (7 FETCH_THIN клиент-рендер вкл. 4 указа-закона; 4 бюджет; 3 enactment;
6 тонких/формат). Исключено по классам: поправки 1655, ратификации 1291, утратили 380,
repealer 59, бюджет 36, амнистии 15. Сводка — `reports/full_corpus_summary.md`; HANDOFF §3
обновлён. Данные вне git (миграция), git-дерево 477→6.5 МБ.

---
## Дожим хвоста (D1-D6, ветка full-corpus)

### D4. «Реестр-баг superseded» — расследовано + укреплено ✅
Факт: в ЗАКОММИЧЕННЫХ файлах реклассификация НЕ терялась (git show по трём коммитам:
K1700000120 TODO@9ef745d → superseded@065a952 → superseded@b41327e(C5); в batch_001 их
нет; corpus_registry.md их не показывает). Наблюдение «остаток-23» = 20 TODO + 3
superseded. НО найден РЕАЛЬНЫЙ латентный класс: дедуп реклассифицировал только
bucket=="TODO", а done присваивается ДО дедупа → старая редакция, однажды попав в
batch_*.csv (C5 как раз начал их читать), застряла бы в done навсегда (двойная
редакция). Укреплено: дедуп теперь реклассифицирует И done-дубли (с done_note),
keep = corpus > done > новейший год; печатный инвариант superseded∩(done|TODO)=∅.
Счёт не изменился (done=288, TODO=20, superseded=3).

### D2. «Статья N .» — диагноз ПЕРЕВЕРНУЛСЯ, фикс в 01_build_article_map ✅
НЕ пробел перед точкой: сырой HTML Z070000234_ = «Статья 1<b>. </b>Основные…» —
точка в ОТДЕЛЬНОМ вложенном <b> (разорванный заголовок). Видимый пробел — артефакт
get_text(' '). Способ 1 видел соседа «Статья 1» БЕЗ точки (мимо), Способ 4 скипал
(якорь внутри <b>). Фикс: ретрай по РОДИТЕЛЮ (re.match СТРОГО в начале — не ловим
«статья N» из тела), только когда первый матч упал (аддитивно).
Регресс: 6 нормальных доков (trudovoy 221, upk 686, koap 1072, Z000000126_ 143,
Z030000461_ 162, Z020000284_ 272) — карты БАЙТ-В-БАЙТ. Z070000234_: 3→29 статей.
Полный конвейер: **DONE, GREEN 1/1**.
