# LEFTOVERS — трупы старого дерева (перенесены 2026-06-11)

Всё ниже лежит в **`_old_tree_leftovers/`** (749MB, в .gitignore). НЕ удалено —
убрано с глаз, чтобы никто не работал со stale-копиями. Рабочее дерево v2 — см. WHERE.md.

| что | было | куда мигрировало в v2 |
|---|---|---|
| `data/final/` (+16 папок `final_backup_*`, `final_pre_links/`) | старый продукт + бэкапы раундов | `final/`; бэкапы текущих скриптов теперь в `backups/` |
| `data/source/` (вкл. `*_files/` браузерных сейвов) | старое сырьё | `source/` |
| `data/maps/`, `config/` | карты и конфиги | `maps/` (слиты) |
| `data/reports/` | старые отчёты | `reports/` |
| `brief/` | ТЗ, чеклисты | `docs/brief/` |
| `tests/` | юнит-тесты | `scripts/tests/` |
| `law_kit/` | передаточный кит | `deliverables/law_kit/` |
| `archive/` (352MB), `anara_review/` (160MB docx) | до-аудитное старьё и ревью-пакеты | без миграции (локальные хранилища) |
| `tmp_b3/`, `ADILETkz_гиперссылки_2026-06-02.zip` | временное R6 / старый архив | — |

## Проверка свежести перед переносом

Сравнение остатков с git-двойниками: **0 случаев «старый новее и отличается»**.
Единственная находка новее v2-миграции: `data/source/mestniySAMOUPRAVLENIE.html`
(+`_files/`) — **выгрузка владельца браузером в СТАРЫЙ путь** (потому Блок 1 R6 её
не нашёл и скачал закон curl'ом сам). Постатейная сверка двух выгрузок: **тексты
61/61 статей идентичны** (единственный дифф — браузерный поиск-виджет в хвосте
страницы, не текст закона). Конвейер построен на curl-версии (`source/`), копия
владельца сохранена в истории git (коммит 008d121) и в `_old_tree_leftovers/`.

## Бонус-эффект

Машинный артефакт «64_final: gettext_frozen=False» на этом компе уходил корнями
в `data/final_backup_ANARA_RECHECK` (древний срез; легаси-фолбэк 64-го его
подхватывал). После переноса фолбэк не находит папку → «нет backup ⇒
by-construction», `verify --all` на этой машине больше не красит 64-й.

## Витрина

`../adilet_public_OLD_NE_TROGAT` — бывшая `adilet_public` (старый экспорт в main).
Заморожена (README_FROZEN.md внутри). Удалять или нет — решение владельца.
