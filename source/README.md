# source/ — сырые выгрузки с adilet.zan.kz

**Руками НЕ редактировать.** Это исходники-эталоны: по ним строится корпус и
сверяется текст (§6 text-invariance). Имя файла может не совпадать со слагом —
соответствие задаёт поле `source` в `maps/codes.json` (напр.
`oGosUslug.html` → слаг `gosuslugi`).

Единственное допущенное изменение в истории: замена внутреннего IP-хоста в
href на adilet.zan.kz (R3 Фаза C, текст не менялся).

Новый документ: положить выгрузку сюда → добавить запись в `maps/codes.json`
→ `python scripts/pipeline/pipeline.py <слаг>` (см. корневой README).
