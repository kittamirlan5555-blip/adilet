# secrets / creds
.env
.env.*
secrets/
credentials*
*.key
*.pem
*.pfx
*.p12
id_rsa*
*.ppk
*.ovpn
*.rdp

# python / env
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# scratch / backups / archives
tmp_*
*.bak_*
*.zip
*.7z
*~
.DS_Store
Thumbs.db

# local agent settings
.claude/

# backup dirs / pre-snapshots (на диске остаются, в репо не идут)
data/final_backup_*/
data/final_pre_links/
data/final_pre_fix11_*

# новое дерево (Фаза A): производные/бэкапы скриптов
interm/
backups/
source/*_files/

# derived/structured_out: jsonl — побайтовые копии derived/chunks/
# (переиздаются scripts/pipeline/structurize.py --all); в гите только QUALITY.md
derived/structured_out/*.jsonl

# heavy local artifacts: ревью-пакеты с docx (ПДн §11), старый архив
anara_review/
archive/
*.docx

# saved-page asset dirs из data/source (мусор браузера, сами .html остаются)
data/source/*_files/

# трупы старого дерева (до v2), убраны с глаз 2026-06-11
_old_tree_leftovers/

# ADILETkz_HANDOFF.md — содержит ФИО/компанию (ПДн §11), репо публичный.
# Исключён из коммита. Каноничный безопасный хэндофф (имена без фамилий) —
# docs/HANDOFF.md. Если нужна эта версия в репо — сначала вычистить ПДн.
ADILETkz_HANDOFF.md

# вектор-слой: ТЯЖЁЛЫЕ перегенерируемые артефакты — на диске остаются, в репо НЕ идут.
# Пересборка: f_full_chunks -> chunks.jsonl; g_build_index -> index.faiss + index_meta.
# Анаре уходят файлом (deliverables/*/chunks/ + zip). В гите — лёгкие config/gaps.
derived/vector_layer/chunks.jsonl
derived/vector_layer/index.faiss
derived/vector_layer/index_meta.jsonl
derived/vector_layer/chunks_top100_backup.jsonl
derived/vector_layer/*.tmp

# машинный отчёт g6-апплая (перегенерируемый r3_02_g6_apply.py)
reports/r3/

# reports/gates: машинные гейт-отчёты verify.py — перегенерируемые (paths.GATES).
# Папка остаётся на диске (verify пишет туда), но в репо не идёт.
reports/gates/
