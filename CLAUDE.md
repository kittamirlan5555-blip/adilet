# CLAUDE.md — проект ADILETkz

> Этот файл — рабочая память проекта для агента (Claude Code). Прочитай его ЦЕЛИКОМ перед любой
> задачей по линковке. Здесь — суть проекта, конвенции, дисциплина проверки и грабли, на которые
> уже наступали. Цель: чтобы любой инстанс Claude (Code или веб) делал корректно и одинаково.

---

## 1. Что это и зачем

Структурирование и гиперлинковка правового корпуса РК (14 кодексов + законы) в HTML — для
публикации на **adilet.kz** и для **vector DB**. Задача простая по смыслу: каждая правовая ссылка
внутри текста («пунктом 2 статьи 35», «Законом РК "О банках…"», «статьёй 100 настоящего Кодекса»)
должна стать рабочей гиперссылкой.

Результат по каждому документу — один файл `*_ready.html` / `*_structured.html` в `ready/`.

---

## 2. Кто есть кто

- **Исполнитель** (Тамирлан) + **ты, агент** — делаете документы.
- **Анара** — ревьюер. Присылает разметку в Word (`.docx`).
  - **ЖЁЛТОЕ выделение = "тут ссылка НЕ горит, а должна". Сделать гиперссылкой полным спаном.**
  - Анара смотрит в Word/тексте, где гиперссылки визуально не отличаются от обычного текста.
    Поэтому **часть её жёлтых — уже залинкованы** (ложные срабатывания). Прежде чем «чинить»,
    проверь реальное состояние в HTML. Совет Анаре/исполнителю: открывать файл в БРАУЗЕРЕ, не в Word.
- **Ергали** — заказчик. Ему уходит финал после Анары.

---

## 2.1 ЖЕЛЕЗНЫЕ ПРАВИЛА РЕВЬЮ

- **Слово Анары — закон.** Её жёлтая подсветка = место СТАНОВИТСЯ ссылкой. Любое внутреннее
  правило («generic-фраза», «нет стабильной цели» и т.п.) ОТМЕНЯЕТСЯ её флагом. Сначала линкуем,
  вопросы потом.
- **«законом о республиканском бюджете» → `Z2500000239`.** Это ЕЖЕГОДНЫЙ закон: каждый декабрь
  обновлять НГР во всех документах (пункт в чеклисте релиза, §12).
- **Сноски (`<span class="note">` «Сноска. …») НЕ линкуем** (правило Анары от 13.05.2026),
  ЕСЛИ ТОЛЬКО она явно не подсветила сноску.
- **Статья 380-3 УК** отсутствует в нашей выгрузке УК (на живом adilet действует) — до обновления
  выгрузки линкуется на КОРЕНЬ `K1400000226`. После обновления — перевести на якорь.
- **Перечни:** каждый номер статьи — ОТДЕЛЬНЫЙ `<a>`; скобки «(частью N)» — вне спана;
  квалификатор «частью третьей» перед «статьи N» — по сложившемуся стилю файла (вне спана для
  внутренних УПК-отсылок); одиночные cross-code — спан включает
  «статьи N Уголовного кодекса Республики Казахстан».

---

## 3. ГЛАВНОЕ правило: ПОЛНЫЙ СПАН

Правовая ссылка = **ОДИН непрерывный `<a>` на ВСЮ фразу**, а не на первое слово.

- ✅ `<a>пунктом 2 статьи 35</a>`   `<a>Законом Республики Казахстан "О банках…"</a>`
- ❌ `<a>пунктом 2</a> статьи 35`   `<a>Законом</a> Республики Казахстан "О банках…"`

Если горит только «пунктом 2» или только «Законом» — это и есть то, что Анара красит жёлтым.
Сюда же: цепочки и диапазоны — «частями второй и третьей статьи 32», «пунктами 4 и 6 статьи 167»,
«статьи 223 – 224» — тоже один спан (для cross-code-списков разных статей — каждый номер отдельно,
см. §4).

**Инвариант:** полный спан НЕ меняет видимый текст — только двигает границы `<a>`. Проверяется (§6).

---

## 4. Типы ссылок и КУДА они ведут (самое важное)

| Тип | Пример | Куда вести | Надёжность |
|---|---|---|---|
| **Внутренняя** | «статьёй 100 настоящего Кодекса» | `#zX` — якорь заголовка статьи в ЭТОМ же файле | ✅ надёжно |
| **Внешний акт целиком** | «в соответствии с Законом РК "О банках…"» | **КОРЕНЬ документа** `https://adilet.zan.kz/rus/docs/{НГР}` (БЕЗ #z) | ✅✅ безопасный дефолт |
| **Cross-code на статью** | «статьёй 258 Уголовного кодекса» | якорь статьи в целевом кодексе `…/{НГР}#zX` | ⚠️ хрупко, см. ниже |

**Правило по умолчанию: внешние акты → КОРЕНЬ.** Корень не зависит от внутренней нумерации, не
ломается никогда.

**Cross-code на конкретную статью — опасная зона.** Якорь обязан совпадать с тем, что реально лежит
на adilet (или в задеплоенном файле). Якоря НАШИХ `_ready.html` НЕ совпадают с живым adilet — у нас
своя (более крупная) нумерация. Пример из УК: у нас `z3100 = ст.125-1`, на живом adilet
`z3100 = ст.430`. Значит article-level cross-link, собранный из нашей карты, на живом adilet ведёт
не туда. **Если не уверен, что якорь совпадёт с целевой средой — веди на корень.** (См. §8.)

---

## 5. Коды актов: `maps/npa_mapping.json` — НО ОН МЕСТАМИ ПРОТУХ

`maps/npa_mapping.json` (название акта → НГР) — отправная точка, **но НЕ источник истины вслепую.**

**Авто-ремап разрешён ТОЛЬКО при подтверждённом repeal-replace:** старый НГР формально отменён И
заменён новым актом ТОГО ЖЕ предмета с ТЕМ ЖЕ названием. Если старый акт ещё действует ИЛИ при
замене сменилось название/предмет (видимая фраза ≠ целевой акт) — **НЕ ремапь**, выпиши на ручной
разбор.

**ПОДТВЕРЖДЕНО (repeal-replace, авто-ремап OK)** — обе цели это doc_id наших же кодексов в
`config/codes.json`, т.е. мы уже на новой редакции:
- Налоговый кодекс: маппинг `K1700000120` (2017) → **`K2500000214`** (2025, наш `nalog`).
- Бюджетный кодекс: маппинг `K080000095_` (2008) → **`K2500000171`** (2025, наш `byudzhet`).

**ТРЕБУЕТ РУЧНОГО РАЗБОРА (НЕ авто-ремап)** — кандидат указан, но сверять на живом adilet:
- О банках: маппинг `Z950002444_` (1995). Кандидат `Z2600000258` (новый закон 2026), но **дата
  вступления в силу не подтверждена** — старый мог ещё действовать. Сверить.
- `Z1500000383` = «Об общественных **СОВЕТАХ**». Кандидат `Z2300000030` = «Об общественном
  **КОНТРОЛЕ**» — **другой предмет/название**. Видимая фраза ≠ целевой акт → ремап ЗАПРЕЩЁН.
- `Z070000252_` = ЗАКОН «О введении в действие Трудового кодекса» (2007) — **это не сам Кодекс**.
  Трудовой КОДЕКС 2015 = `K1500000414`. Разные акты — проверять контекст фразы вручную.

**НЕ протухло (частая ошибка — не путать):**
- `Z1400000211` = «О профессиональных **СОЮЗАХ**» (№211-V, 27.06.2014) — **ДЕЙСТВУЮЩИЙ**, не ремапь.
- «О профессиональных **квалификациях**» = отдельный действующий акт `Z2300000014` (№14-VIII,
  04.07.2023). Оба уже корректны в `npa_mapping.json`.

**ПРАВИЛО: перед линковкой проверь, что код — действующая редакция** (акт не отменён/не заменён).
Как: открой `adilet.zan.kz/rus/docs/{НГР}`, убедись, что это действующий акт с нужным названием; для
недавно заменённых — найди новый НГР (поиск по названию + год). Свежесобранные вручную коды
исполнителя обычно новее маппинга — им верь больше.

> TODO проекта: пересобрать `npa_mapping.json` на актуальные редакции и прогнать аудит ВСЕХ внешних
> adilet-ссылок во всех готовых документах (могут быть линки на отменённые акты).

---

## 6. Дисциплина проверки — НЕ доверять «готово»

`sha`/`audit`/`get_text`-гейты **слепы** к корректности якорей, к границам спанов и к cross-code.
Они скажут «ок», когда ссылка ведёт на чужую статью. Проверять надо **независимо из сырого HTML**:

1. **text-invariance:** `''.join(soup.get_text().split())` байт-в-байт ДО == ПОСЛЕ.
   - ВАЖНО: `get_text(' ')` (с разделителем) МЕНЯЕТСЯ при сдвиге границ `<a>` — это артефакт.
     Истина — `get_text()` БЕЗ разделителя, ws-stripped.
2. **Якоря резолвятся:** каждый `#z` имеет цель `id=`/`name=` в документе.
3. **Cross-code сверка:** для каждой ссылки на статью целевого кодекса — якорь ведёт на статью с
   ТЕМ ЖЕ номером (построй карта `статья→якорь` ИЗ целевого `_ready.html`, сверь).
4. **Нет:** двойных `href`, вложенных `<a>` (`a.find('a')`), оставшихся плейн-ссылок из флагов Анары.
5. **Спот-чек:** руками открой 5-10 ссылок, убедись что ведут куда надо.

Урок, политый кровью: `КОРПУС ПОДТВЕРЖДЁН` от audit-скрипта ≠ корректно. Мислинки (206→чужая
статья, 147→z587 вместо z584) проходили все гейты и ловились ТОЛЬКО независимым осмотром HTML.

---

## 7. Пайплайн нового документа (закон/кодекс)

1. `11_structure_html` — структура: заголовки статей, якоря `#zN`.
2. `01_build_article_map` — карта `статья → якорь`.
3. `02_fix_internal_links` — внутренние ссылки.
4. `07_add_subpoint_anchors` — якоря пунктов/подпунктов.
5. `68_link_canon --doc-id {НГР}` — каноническая линковка (само-ссылки → `#z`, dedup, repoint).
6. `71_fullspan_wrap` — полный спан одиночных «пункт N статьи M».
7. **ВЕРИФИКАЦИЯ:** `64_final_verify` / `67_independent_verify` / `69_sixcheck_laws` + ОБЯЗАТЕЛЬНО
   независимая проверка из сырого HTML (§6).
8. Применить замечания Анары: жёлтое → гиперссылка полным спаном; внешние акты → корень (§4),
   код сверить на актуальность (§5).

---

## 8. Что СЕЙЧАС делается вручную (надо зашить в скрипты)

Скрипты дают ~97-99% ссылок, но НЕ покрывают (это и есть «хвост», который красит Анара):

- **внешние акты → корень**: полный спан всей фразы «Законом РК "Название"» → `docs/{НГР}` (root).
- **цепочки/диапазоны в полном спане**: «пунктами N и M статьи K», «частями… статьи K», «статьи N–M».
- **проверка актуальности кода** (протухший маппинг, §5).
- **cross-code сверка якорей** с целевым кодексом (§6.3).

**Приоритет на «сделать ахуенно»:** вынести эти 4 пункта в скрипты (напр. `72_external_root_link.py`,
`73_fullspan_chains.py`, `74_code_freshness_check.py`, `75_crosscode_verify.py`) — тогда ручного
труда почти не останется, и любой документ закрывается пайплайном.

---

## 9. Открытый архитектурный вопрос (решить с Ергали)

Внешние/cross-code ссылки ведут на `adilet.zan.kz`. Якоря наших `_ready.html` ≠ якоря ЖИВОГО adilet
(разные схемы нумерации). Поэтому:

- **Вариант A:** на adilet деплоятся НАШИ `_ready.html` (заменяют страницы) → якоря совпадут, всё
  работает. Тогда cross-code на статью корректен.
- **Вариант B:** ссылки должны вести на СУЩЕСТВУЮЩИЕ страницы adilet → якоря надо строить под их
  схему (нужен реальный HTML их страниц; adilet блокирует ботов — берём у исполнителя).

Пока модель не подтверждена — для внешних актов **веди на КОРЕНЬ** (там разницы нет), а article-level
cross-code помечай как зависящий от модели деплоя.

---

## 10. Структура репо

```
ready/codes/   — *_ready.html (кодексы)
ready/laws/    — *_structured.html (законы)
maps/          — npa_mapping.json (акт→НГР) + codes.json + article_map_* + subpoint_map_*
scripts/       — канонические скрипты (нумерованные)
source/        — текстовые выгрузки разметки Анары
docs/          — RUNBOOK, и т.п.
README.md      — обзор + статус-таблица (для людей)
CLAUDE.md      — этот файл (для агента)
```

---

## 11. Безопасность (репо ПУБЛИЧНЫЙ)

Перед КАЖДЫМ коммитом — скан на секреты. НЕ коммитить: креды/токены/.env/*.key/*.pem, внутренние
IP/эндпоинты (напр. `<внутренний-ip>:9096`), VPN/RDP-конфиги, персданные (ИИН/ФИО/номера дел),
бинарные docx с метаданными. `.gitignore` держать актуальным. Сами правовые тексты — публичные
(adilet), их можно.

---

## 12. Краткий чек-лист «закрыл документ»

- [ ] все флаги Анары (жёлтое) → горят полным спаном
- [ ] внешние акты → корень; коды сверены на актуальность
- [ ] cross-code: якоря сверены с целевым кодексом (или помечены как зависящие от деплоя)
- [ ] text-invariance ✅ (без разделителя)
- [ ] якоря резолвятся, нет двойных href / вложенных `<a>` / плейн-остатков
- [ ] спот-чек 5-10 ссылок руками
- [ ] секретов в коммите нет
- [ ] (релиз, КАЖДЫЙ ДЕКАБРЬ) НГР ежегодного закона «о республиканском бюджете» (сейчас
      `Z2500000239`) актуализирован во ВСЕХ документах (§2.1)
