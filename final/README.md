# final/ — рабочие финальные формы корпуса

`{слаг}_ready.html` — плоская форма со ссылками; `{слаг}_structured.html` —
она же с иерархией `div[data-type]` (КАНОН по ссылкам). Видимый текст обеих
форм идентичен и равен source (§6.1).

**Руками не редактировать без гейтов.** Любая правка — только скриптом с
text-invariance-гейтом и проверкой G6 (синхронность форм):
`python scripts/audit/71_gates.py <слаг> --strict`.

**ЗАПРЕЩЕНО перегенерировать принятые документы** (пайплайн сотрёт ручные
правки раундов) и трогать документы, находящиеся на ревью у Анары
(см. reports/WAITING_ON_HUMANS.md).
