# deliverables/ — сдаточные пакеты по раундам

Только то, что отправлялось/отправляется людям. Содержимое пакетов —
СНАПШОТЫ на момент сдачи, не синхронизируются с final/ автоматически.

- `laws3/` — три закона R2 (НА РЕВЬЮ У АНАРЫ — не трогать).
- `laws_r3/` — три новых закона R4 + SDACHA_R3.md (у Анары — не трогать).
- `anara_package/` — майский пакет Анаре (чанки, деревья, ПРИМЕРЫ).
- `send_chef/` — майский пакет шефу (эталон формата hier_id).
- `law_kit/` — поставляемый набор скриптов для исполнителя (снапшот;
  обновляется при передаче, не синхронизирован с scripts/).

Новый пакет = новая папка раунда + SDACHA_*.md с цифрами и решениями.
